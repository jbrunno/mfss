import { tv } from '@kirvanobr/theme'

export const styles = tv({
  slots: {
    base: ['w-full mx-auto text-center flex flex-col justify-center gap-4'],
    iconWrapper: ['mx-auto text-gray-200/50'],
    textsWrapper: ['text-gray-400 space-y-1'],
  },
})
